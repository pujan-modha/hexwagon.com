// DEPRECATED: This page showed scheduled tools which is not part of HexWagon's core model.
// Redirects to home for now.
import { redirect } from "next/navigation"

export default function ComingSoonPage() {
  redirect("/")
}
